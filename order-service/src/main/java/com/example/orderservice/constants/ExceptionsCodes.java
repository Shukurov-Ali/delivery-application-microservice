package com.example.orderservice.constants;

public interface ExceptionsCodes {

    String ORDER_NOT_FOUND_CODE = "order.not.found";
    String ORDER_ALREADY_DELIVERED_CODE = "order.already.delivered";
    String ORDER_INVALID_STATUS_CODE = "order.invalid.status";
    String ORDER_ALREADY_PICKED_UP_CODE = "order.already.picked.up";
    String VALIDATION_ERROR_CODE = "validation.error";
}
